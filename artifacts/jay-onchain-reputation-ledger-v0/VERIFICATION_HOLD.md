# Verification HOLD

The persisted V0 branch is not promoted green until the event-class constraints in the schema are verified against the frozen semantics document.

No ingest is authorized by this branch.
